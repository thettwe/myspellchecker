"""Data generation for Soft-Masked BERT training: clean sentences → corrupted/gold pairs.

Named ``datagen`` rather than ``data`` because the repo's top-level ``.gitignore``
treats every ``data/`` path as ignored runtime artifact. Importable via
``myspellchecker.training.softmasked.datagen``.
"""
